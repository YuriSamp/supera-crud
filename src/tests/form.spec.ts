import { describe, expect, test } from 'vitest';
import { render, fireEvent } from './test-utils';
import userEvent from '@testing-library/user-event';

describe('form test', () => {
  test('Should give me an error in name field', () => {});
});
